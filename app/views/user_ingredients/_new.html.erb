<h1>New Ingredient</h1>

<ul id='category_list' class='ajax_list'>
<% @ingredient_categories.each do |category| %>
	<li id=<%=dom_id(category)%> class='highlightable'>
	<a<div class='section'> * <%=category.name%></div>
	<div class='category_items'> </div>
	</li>
<%end%>
</ul>

<%= link_to 'Back', user_ingredients_path %>
